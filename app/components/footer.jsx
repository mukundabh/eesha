define(function (require) {
  const React = require('react');

  return React.createClass({
    render: function () {
      return (
        <footer>
          <h1>Eesha Footer</h1>
        </footer>
      );
    }
  });
});
