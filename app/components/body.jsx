define(function (require) {
  var React = require('react');

  return React.createClass({
    render: function () {
      return (
        <footer>
          <h1>Eesha Body</h1>
        </footer>
      );
    }
  });
});
